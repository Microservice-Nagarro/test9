apiVersion: v1
kind: ConfigMap
metadata:
  name: mymicroservice-configmap # Change me
data:
  appsettings.Production.json: |-
    {
      "ConnectionStrings": {
        "KeyVaultUri": "#{KeyVaultUri}#",
        "ManagedIdentityClientId": "#{ManagedIdentityClientId}#"
      }
    }